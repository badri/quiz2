<?php

/**
 * @file
 * Provides multichoice question.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function multichoice_question_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'question') {
    return "plugins/$plugin_type";
  }
}
